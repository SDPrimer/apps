{
  "name": "@shared/constants",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "tsc -b",
    "build:watch": "tsc -b --watch",
    "lint": "eslint ."
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "eslint": "^9.27.0"
  }
}
